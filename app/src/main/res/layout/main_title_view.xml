<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dip"
    android:background="@color/text_color_green" >

    <RelativeLayout
        android:id="@+id/rl_search"
        android:layout_width="match_parent"
        android:layout_height="38dip"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="10dip"
        android:layout_marginTop="6dip"
        android:layout_toLeftOf="@+id/btn_download_manager"
        android:background="@drawable/search_edit_bg" >

        <ImageView
            android:id="@+id/search_icon"
            android:layout_width="25dip"
            android:layout_height="25dp"
            android:layout_centerVertical="true"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="10dip"
            android:background="@drawable/search_bar_btn_left" />

        <TextView
            android:id="@+id/tv_search_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginLeft="5dip"
            android:layout_toRightOf="@id/search_icon"
            android:text="@string/tv_search_hint"
            android:textColor="@color/text_color_grey"
            android:textSize="16sp" />

        <ImageView
            android:layout_width="40dip"
            android:layout_height="40dip"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:scaleType="center"
            android:src="@drawable/title_scan" />
    </RelativeLayout>

    <ImageButton
        android:id="@+id/btn_download_manager"
        android:layout_width="48dip"
        android:layout_height="48dip"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:padding="12dip"
        android:background="@drawable/title_ibtn_bg"
        android:src="@drawable/pp_icon_home_download_arrow" />

</RelativeLayout>